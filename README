# Wordle kt

A wordle solver written in kotlin/js.

This project creates a web app that works with you to find the next best wordle word to guess based on the existing knowledge.

## Building and Running

To build the project
```bash
./gradlew build
```
The built project will be in ./build/distributions.

To run the project locally
```bash
./gradlew run --continuous
```
